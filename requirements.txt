# Core async
aiohttp
websockets

# Stream handling
streamlink

# API clients
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# NLP / sentiment
vaderSentiment

# Cryptography (Fernet token encryption)
cryptography

# Queue & cache
redis

# Storage
boto3
google-cloud-storage

# Web / dashboard API
fastapi
uvicorn[standard]
python-multipart
itsdangerous

# Auto-captions (Whisper on the droplet). NOT imported at startup — the model
# is loaded lazily on the first caption job — so a box without this installed
# still boots and clips normally; only /uploads/{id}/captions fails.
# Deploys do not run pip install: `venv/bin/pip install faster-whisper` on the
# droplet before setting CAPTIONS_ENABLED=true.
faster-whisper

# Utilities
pydantic
pydantic-settings
python-dotenv
structlog
tenacity
httpx

# Testing
pytest
pytest-asyncio
pytest-mock
stripe
