# Core async
aiohttp==3.9.5
websockets==12.0
asyncio-mqtt==0.16.2

# Stream handling
streamlink==6.7.4
ffmpeg-python==0.2.0

# API clients
twitchAPI==4.2.0
google-api-python-client==2.130.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# NLP / sentiment
vaderSentiment==3.3.2

# Queue & cache
# NOTE: the async client lives in `redis.asyncio` (redis-py >= 4.2);
# the abandoned `aioredis` package was removed — it fails to import on Python 3.11.
redis==5.0.4
rq==1.16.2

# Database (uncomment after installing PostgreSQL)
# asyncpg==0.29.0
# sqlalchemy[asyncio]==2.0.30
# alembic==1.13.1
# psycopg2-binary==2.9.9

# Storage
boto3==1.34.100
google-cloud-storage==2.16.0

# Web / dashboard API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
itsdangerous==2.2.0

# Utilities
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
structlog==24.1.0
tenacity==8.2.3
httpx==0.27.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-mock==3.14.0
